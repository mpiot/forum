{{ form_start(form) }}
    {{ form_row(form.email) }}
    {{ form_row(form.plainPassword) }}
    {{ form_rest(form) }}

    <button class="btn">{{ button_label|default('Register') }}</button>
{{ form_end(form) }}
